micronaut:
  application:
    name: micronaut
---
micronaut:
  security:
    endpoints:
      login:
        enabled: true
      logout:
        enabled: true
    session:
      login-success-target-url: /
      login-failure-target-url: /login/authFailed
    login-handler: session
